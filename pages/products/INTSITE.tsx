import { ReactElement } from 'react';

import Layout from 'components/Layout';

const Intsite = (): ReactElement => (
  <Layout title="INTSITE">
    <h1>INTSITE product page</h1>
  </Layout>
);

export default Intsite;
